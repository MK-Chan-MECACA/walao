{
  "name": "walao",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WALAO — personal AI information layer for WhatsApp (ticket 1: ingestion walking skeleton)",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --env-file=.env --watch src/server.ts",
    "seed:dev": "node --env-file=.env scripts/seed-dev.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.0"
  }
}
